This file lists the Python libraries needed by app.py
The deployment server (Render) reads this file to install the required packages.
1. The core web framework
Flask==2.3.3

2. Used for making HTTP requests (e.g., to the TMDb API)
requests==2.31.0

3. Used for handling cross-origin requests from your frontend
Flask-Cors==4.0.0

4. The production-ready server that Render uses to run the Flask app
gunicorn==21.2.0